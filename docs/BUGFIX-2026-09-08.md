# Bugfix-Pass Aegis 2.1.205 — 2026-09-08 (Pass 6, Code)

Kein Merge von `bugfix` (2.1.15). Kein CI. MatchMath.swift ~357 kB — keine Flag-für-Flag-Schleife.

## Warum es schlecht funktioniert

Aegis ist kein Tracker. Es ist ein Stapel leftover-Maps plus Flags seit 2.1.100.

1. **Vier Schlüsselräume für ein Gesicht.** Live-UUID, Hamming-Hash, Pose-Bin, PairLast. AssignLive, HoldMove, TickCopy, OccupiedMerge schreiben sie nicht atomar.
2. **AssignAtomic wischte Coast.** 2.1.204 baute `leftoverAssignAtomicAll` so, dass nur `liveToHold` überlebte. Coast/Twin nach einem Remint tot — deshalb unverdrahtet.
3. **Occupied-Yaw random UUID.** `UUID(uuidString:) ?? UUID()` → Yaw 0, Twin-Rank Glück.
4. **Gallery linear.** Jeder Frame × alle Prints.
5. **JPEG-Roundtrip.** Detect+Print teilen keinen CVPixelBuffer.
6. **Main-Thread CGImage.** Detect bleibt teuer.
7. **Orakel-Tests.** Grün heißt: der letzte Schalter kompiliert.
8. **Zwei Pixelpipelines.** 420v → BGRA → JPEG.

## Pass 6 — gelandet (2.1.205)

| # | Bug | Fix |
|---|---|---|
| 1 | AssignAtomic droppt Coast | Keep unassigned, alter Hold-Key tot |
| 2 | remintPlan stored→live | `leftoverAssignAtomicRemint` |
| 3 | leftoverTracks tot | Pack + LibraryStore nach remintPlan |
| 4 | Gallery linear | `leftoverPrintCandidates` Bin × Name |
| 5 | Twin-Taufe | `leftoverTwinLockHolds` 0,90 / 15° |
| 6 | Occupied-Yaw random | `leftoverOccupiedYaw` |

2.1.204 bleibt: Rescue facesInFrame, GhostDrop Occupied.

Kein neues leftover*-Flag für Cosine. Nächster Code-Schritt: leftoverTracks = einzige Map (leftoverFaceTrackRemintDropMaps aus Unpack), lokal auf dem Mac.
