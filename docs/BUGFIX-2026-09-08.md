# Bugfix-Pass Aegis 2.1.207 — 2026-09-08 (Pass 8)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 8 — neu gefunden

1. **leftoverPairLast nach Remint nur faceMaps.** `leftoverTracksUnpack.pairLast` (Hold-ID) wurde verworfen. Twin/Coast und Assign schrieben zwei Wahrheiten.
   Fix in `LibraryStore` nach `leftoverTracksUnpack`:
   `leftoverPairLast = unpacked.pairLast.isEmpty ? faceMaps.pairLast : unpacked.pairLast`
2. **leftoverTracks nicht einzige Live-Map.** Yaw/Kalman/Blink weiter faceMaps.
3. **JPEG-Roundtrip Detect+Print.** Unverändert.
4. **LiveCapture @MainActor.** Unverändert.

Drei Folge-Prüfungen auf dem Assign-Block: Unpack gewinnt; leere Map fällt auf faceMaps; kein neues leftover*-Flag.
