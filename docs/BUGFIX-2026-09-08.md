# Bugfix-Pass Aegis 2.1.209 — 2026-09-08 (Pass 10b)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 10b — gelandet

1. **uniqueID sticky.** 2.1.208 wischte Kalman bei jedem Continuity-Reconnect. Name+Role, Mac nie. Osmo ≠ Mac.
2. **Hung spawn cap 2.** 2.1.208 startete nach 1 s unbegrenzte VNImageRequestHandler. Stale-Gen drain pending.
3. **Print-Cache yaw nil tot.** `liveYaw ?? 0` skippte den ersten Print als Frontal.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — sticky iPhone hält Kalman; spawn cap 2; yaw nil druckt. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.208 — 2026-09-08 (Pass 10)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 10 — gelandet

1. **Hung-Detect Cancel nach 1 s.** Vision-Detect hängt, `liveBusy` ewig, pending nur overwrite. `liveEmitHungCancel` + `liveDetectGen` invalidiert den stecken Task, `markFrameConsumed`, neuer Detect auf dem aktuellen Frame.
2. **Print-Cache leftoverLastHash × Yaw-Bin.** Gleicher Hash frontal übersprang VNFacePrint im Profil. `leftoverLastHashBinKey` / `leftoverPrintCacheHits` / `Put`. Skip nur gleicher Bin. uniqueID-Wechsel und `stopLive` wischen.
3. **JPEG-Probe × Yaw-Bin.** `leftoverJpegProbeStore` nur Spatial — Frontal-Probe auf Profil. `StoreBin` schreibt `#bin`, `LookupBin` Fallback Spatial nur Bin 0.
4. **Blink-Liveness auf Assign.** `leftoverAssignPrintCell` rief `leftoverAssignBlinkOk(..., alreadyNamed: true)` — Hungarian taufte Unnamed ohne Blinzeln. Zelle 0 wenn `blinkOk` fehlt (`alreadyNamed: false` intern). Call-Site `leftoverBlinkSeen(faceId:)`.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — Hung 1 s startet neu; Profil nach Frontal druckt; Unnamed ohne Blink kein UUID.
