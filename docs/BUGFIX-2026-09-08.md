# Bugfix-Pass Aegis 2.1.206 — 2026-09-08 (Pass 7, Code)

Kein Merge von `bugfix` (2.1.15). Kein CI. MatchMath.swift ~357 kB — keine Flag-für-Flag-Schleife.

## Warum es schlecht funktioniert

Aegis ist kein Tracker. Es ist ein Stapel leftover-Maps plus Flags seit 2.1.100.

1. **leftoverTracks tot nach Pack.** 2.1.205 packte und remintete leftoverTracks, schrieb Identität weiter aus leftoverFaceTrackRemintDropMaps — zweite Write-Quelle, Coast/Twin wieder auseinander.
2. **Print-Index nur Math.** FaceEngine.matchLive zählte weiter alle Fotos.
3. **Twin-Lock nur Math.** leftoverAssignPrintCell taufte Twins (cosine 0,93, Yaw gleich).
4. **JPEG-Roundtrip.** Detect+Print teilen keinen CVPixelBuffer.
5. **Main-Thread CGImage.** Detect bleibt teuer.

## Pass 7 — gelandet (2.1.206)

| # | Bug | Fix |
|---|---|---|
| 1 | Tracks unverdrahtet | `leftoverTracksUnpack` → leftoverHold / LastHash / NameLock / CoastAt |
| 2 | Mirror 20 Writes | `leftoverTracksMove` in leftoverMirrorPending |
| 3 | Gallery linear | `leftoverPrintCandidates` in FaceEngine.matchLive (Bin × Name) |
| 4 | Twin-Taufe | leftoverAssignPrintCell Twin-Lock → 0 statt Cosine |

2.1.205 bleibt: AssignAtomic Coast-Keep, Remint stored→live, Print-Index Math, Twin-Lock Math, Occupied-Yaw.

Kein neues leftover*-Flag. Nächster Code-Schritt: CameraBroker oder leftoverTracks als einzige Map auch für Live-Skalare (Yaw/Kalman), lokal auf dem Mac.
