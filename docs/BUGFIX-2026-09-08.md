# Bugfix-Pass Aegis 2.1.207 — 2026-09-08 (Pass 9)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 9 — gelandet

1. **leftoverAssignAtomicAll clobberte pairLast.** `t.pairLast = hold` machte last-proposed = Remint-Source. Twin/Coast und Assign zwei Wahrheiten. Fix: Value remintet über holdToLive, sonst bleibt.
2. **leftoverPairLast nach Remint nur faceMaps.** Unpack verworfen. Fix: `leftoverPairLastPick` — Unpack gewinnt, leer fällt auf faceMaps.
3. **leftoverTracks ohne Live-Skalare.** Yaw/Kalman/Blink 20 Drops, nach Remint divergiert. Fix: Pack yaw/velX/velY/blink, Unpack nach AtomicRemint, MapPick.

Drei Folge-Prüfungen: pairLast fremd bleibt; Self remintet auf live; Yaw 0,35 überlebt. Kein neues leftover*-Flag.
