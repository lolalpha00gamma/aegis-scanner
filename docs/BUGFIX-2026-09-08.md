# Bugfix-Pass Aegis 2.1.225 — 2026-09-08 (Pass 26)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 26 — gelandet

Pass 25: PTS-Fill, Palm-skipPrint, Twin P-Slot. Danach mapte `abs(yaw)` ¾L auf ¾R-Hold, Print-Cache mischte Built-in/Continuity, `@cam` zerlegte `Int(bin)`, Gast-Drop war still, FA-JSONL las Frontal-Hold in Profil.

1. **Yaw L/R Hold.** `leftoverHoldPrevOf` / `leftoverHoldBinPut` / `leftoverTrailNowOf` mit signed Yaw. ¾L liest nicht ¾R.
2. **Print-Cache × Camera.** Key `hash#bin@cam`. Built-in-Print vs Continuity nicht mischen.
3. **Print-Cache Bin-Parse.** `leftoverPrintCacheBin` strippt `@cam`. SM-Bins und YAW-Coverage waren tot.
4. **Legacy-Hit.** Alter Disk-Key ohne `@cam` gilt nach Update.
5. **Guest-TTL Chip.** Overlay-Countdown 30 s, `GUEST drop`.
6. **FA-Hold × Yaw.** `leftoverHoldNow` in JSONL nimmt liveYaw, nicht Frontal-Default.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverOccupiedHashes noch abs.

Loop: drei Folge-Prüfungen — ¾L Hold ≠ ¾R; YAW ●●● mit @cam-Keys; Gast-Chip zählt. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.224 — 2026-09-08 (Pass 25)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 25 — gelandet

1. **Mutex PTS Wall + Fill.** `obsFillUsesMutexPts` war getestet, nie am FrameTap. Helios schrieb Media-PTS. Jetzt Unix-Wall + Fill in FrameTap.
2. **Helios Palm skipPrint.** Palme aus der Lock-Zeile, Pixel-Box, IoU 0,18. Nur Continuity + Holder Helios — Built-in UV passt nicht.
3. **enrollSMReadyFromChip needProfile.** `●●●●` ist Prefix von `●●●●●`. Twins in `twinSplits` brauchen P-Slot vor Taufe.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — Frame-Stamp = Mutex-Unix wenn Skew klein; Helios-Palme skippt Continuity-Print; Ada↔Ben tauft Unnamed nicht ohne P. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.223 — 2026-09-08 (Pass 23)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 23 — gelandet

1. **P-Slot.** leftoverEnrollSlotHave |yaw|≥0,70 → profile. Bins ±2 nicht mehr ¾. enrollSMChip Front→¾L→¾R→P→Blink.
2. **enrollSMSkipCapture haveProfile.** Profil-Yaw skippt nur wenn P voll, nicht weil ¾L/R da war.
3. **Twin-Split persist.** twinSplitKey/Blocks/Insert/Cull. UserDefaults `aegis.twinSplits`. leftoverPick remaining gefiltert.
4. **Yaw-Kompass.** enrollYawCompass Overlay.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — Profil-Yaw schreibt P nicht ¾; FA Ada→Ben×3 blockt Taufe nach Restart; Chip ●●●●● wenn P+Blink. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.221 — 2026-09-08 (Pass 22)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 22 — gelandet

1. **leftoverPickFloor.** Argmax/Lookalike/Ambiguous = min(roh, Median). Spike 0,91/0,72 → 0,72. Coast -1 → Hold. leftoverPickPrint bleibt roh für Detect-Skip.
2. **People-Seed need 3.** Callsite war hart `need: 1` — 1-Still-Alben luden bevor SM-Gate greift.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — Spike 0,91 tauft nicht; People 1 Still tot; SM-Gate hält Front+L+R. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.220 — 2026-09-08 (Pass 21)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 21 — gelandet

1. **Print-Cache persist.** `leftoverPrintCacheEncode` / Decode in gallery.json. RAM tot nach Restart, Vision druckte alles neu.
2. **People-Seed SM Hard-Gate.** `peopleAlbumSMBlocksSeed` — ohne Front+L+R kein Seed (3× Frontal).
3. **Auto-Split Twins.** `twinAutoSplit` FA-Matrix Ada→Ben ×3 → Chip `SPLIT Ben≠Ada`.
4. **Mask Twin-Veto.** `maskTwinVeto` Maske vs volles Template, Floor 0,78.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — Restart ohne Re-Print; People ohne Profil skippt; Maske 0,64 tauft nicht. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.219 — 2026-09-08 (Pass 20)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 20 — gelandet

1. **Cosine 3-Tick Median.** `cosineTickMedian` in `leftoverHoldSmooth(trail:)`. Spike 0,91/0,70/0,72 → 0,72, nicht EMA 0,78.
2. **Enrollment Liveness Pflicht.** `enrollSMBlocksName` — Unnamed ohne Front+¾L+¾R+Blink tauft nicht. Chip allein ließ 1 Frontal durch.
3. **Merge-Undo 30 s.** `mergeUndoHolds` / `mergeUndoChip` nach People-Dup Skip.
4. **Capture-Quality Spark.** `captureQualitySpark` Overlay `CQ n · skip`.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — Spike tauft nicht; ENROLL F kein Name; People-Dup zeigt UNDO 30s. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.218 — 2026-09-08 (Pass 19)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 19 — gelandet

1. **Lookalike Negative-Embed.** `lookalikeNegative` Gap < 0,04 bei 0,80/0,76. leftoverPick + rank. Twin tauft nicht.
2. **Night/IR Luma-Gate.** Continuity Luma < 0,12 skippt `stampPrints`. Nacht-IR vergiftete die Galerie.
3. **Guest-TTL 30 s.** `pruneGuestTTL` vor persist, lastSeen hält Live.
4. **Hidden-Album skip.** Title + subtype 205. People seedet nicht Ausgeblendet.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — Ada/Ben 0,91/0,88 kein Name; dunkles Continuity ohne Print; Hidden-Album leer. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.217 — 2026-09-08 (Pass 18)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 18 — gelandet

1. **Temporal Majority-Vote 5 Frames.** `nameTemporalNeed` Floor 5. 8 fps Burst von 3 Ticks tauft nicht. Overlay + Lock.
2. **VNDetectFaceCaptureQuality Print-Skip.** `printCaptureQualitySkip` in `stampPrints`. Groß+unscharf (size ≥ 0,16) vergiftete die Galerie.
3. **Duplicate-Identity Detector People-Seed.** Cosine ≥ 0,89 gegen bestehende ID → Skip, nicht zweite Ada.
4. **Enrollment-Quality-Meter.** Chip `Q n · schwach/gut` live.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — 3 Continuity-Ticks taufen nicht; unscharfe Close-Up ohne Print; People legt keine zweite Ada an. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.216 — 2026-09-08 (Pass 17)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 17 — gelandet

1. **Track lost → Detect sofort.** `overlayTrackLost` / `overlayTrackForcesDetect`. Confidence < 0,15 startet Detect, nicht Kalman-Coast.
2. **People-Album Yaw-Diversität.** Scan 12, Pick Front+L+R. Nicht 3 Frontal.
3. **Photos limited-auth.** `.any` statt Faces-Album. Status „eingeschränkt“.
4. **FA-Heatmap Matrix-UI.** `falseAcceptPairMatrix` neben Chip.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — lost Track startet Detect; People legt Front+Profil an; limited-auth nicht leer. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.215 — 2026-09-08 (Pass 16)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 16 — gelandet

1. **VNTrack Observation persist.** skipDetect hält Sequence+Observation. Detect reseedet via `seedTrack`. One-shot Handler war tot Track.
2. **Enroll-SM skippt volle Bins.** `enrollSMSkipCapture` schreibt skipIds — kein zweites Frontal-Print.
3. **People-Album 3 Stills.** Detect + FaceIds, limited-auth. Nicht nur Name.
4. **FA-Replay Pair-Heatmap.** Chip Ada→Ben×n. Kein neues leftover*-Flag.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — skipDetect-Box folgt ohne Detect; ENROLL F skippt wenn Front voll; People legt Referenzen an, nicht leere IDs. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.214 — 2026-09-08 (Pass 15)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 15 — gelandet

1. **VNTrackObjectRequest Box zwischen Detect.** Lerp folgte der toten Detect-Box. `FaceEngine.trackBoxes` + `overlayTrackStep` IoU-Fuse. Handler pro Aufruf, nicht Prozess-weit.
2. **PhotoKit People-Album Enroll-Seed.** `peopleAlbumEnrollOk` + Photos Faces-Album. Toolbar People.
3. **False-Accept JSONL Replay.** expected=Lock, decided=Vote. Nur Assignment-Wechsel, Cap 500. Toolbar FA-Log. Kein neues leftover*-Flag.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — skipDetect-Box folgt dem Kopf; People legt Identitäten an; FA-Log zeigt Lock≠Vote. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.213 — 2026-09-08 (Pass 14)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 14 — gelandet

1. **Mac kein 720-Preset.** `sessionPresetApplies720` nutzt `external`. FaceTime blieb 720 nach Pass 13.
2. **Softmax-Temp × Galerie.** `gallerySoftmaxTemp`. 8 Identitäten weicher, Impostor tauft nicht.
3. **Overlay reduced-motion.** tau 0 = Snap. 60 Hz Lerp aus.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — Mac Chip 1080@30; 8-Personen Softmax blockt 0,80/0,70 nicht härter als 2; Reduce-Motion Box snap. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.212 — 2026-09-08 (Pass 13)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 13 — gelandet

1. **Osmo Format-Leiter 720@24.** `captureLockFrameRate(..., external:)` lockt 24 wie Continuity. 30-Request auf USB fiel auf 8. 720-Preset bleibt (nicht Continuity-skip).
2. **Enroll-SM Chip.** Front → ¾L → ¾R → Blink. Toolbar `ENROLL F` treibt den Schritt. Bins + Blink. Kein neues leftover*-Flag.
3. **Box-Track 60 Hz.** `overlayTrackDt` + Timer-Beat. Lerp unabhängig von Detect 8–24. Hung-Coast bleibt Origin.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — Osmo Chip `420f 15–24` nicht BGRA 8; ENROLL-Chip nach Profil; Box gleitet zwischen Detect. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.211 — 2026-09-08 (Pass 12)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 12 — gelandet

1. **Osmo CameraChoice.** Picker + `preferredCamera`. Early-Return war `choice != .continuity` → Osmo bekam FaceTime. `cameraChoiceSkipsBuiltIn`.
2. **Hung-Coast ohne Kalman-Write.** Overlay aus last-real + Vel × elapsed. `skipDetect` schreibt Kalman/Vel nicht — sonst Flip nach Detect.
3. **Yaw-Coverage-Meter.** Print-Cache Front+L+R. Chip `YAW ●●●` live.
4. **liveCoastElapsed** Origin hält über Hung-Spawn. Cap 1 s / 0,28.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — Osmo wählt Extern nicht FaceTime; Hung-Box coastet ohne Snap-Back; YAW-Chip nach Profil. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.210 — 2026-09-08 (Pass 11)


Kein Merge von `bugfix`. `[skip ci]`.

## Pass 11 — gelandet

1. **Hung Overlay coast.** 1 s+ Vision-Hänger ließ Boxen stehen. Coast aus Kalman, dann Spawn.
2. **liveHungSpawnOk inflight 0.** `liveBusy` klebte ohne Task — Spawn war tot.
3. **cameraNameBare.** uniqueID sticky strippt ` · Tiefe` wie Helios.
4. **Sleep/Wake Session-Recovery.** `recoverAfterWake` startet die Session, Kalman bleibt.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — Hung Box bewegt sich; inflight 0 recovered; Wake ohne Kalman-Dump. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.209 — 2026-09-08 (Pass 10b)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 10b — gelandet

1. **uniqueID sticky.** 2.1.208 wischte Kalman bei jedem Continuity-Reconnect. Name+Role, Mac nie. Osmo ≠ Mac.
2. **Hung spawn cap 2.** 2.1.208 startete nach 1 s unbegrenzte VNImageRequestHandler. Stale-Gen drain pending.
3. **Print-Cache yaw nil tot.** `liveYaw ?? 0` skippte den ersten Print als Frontal.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — sticky iPhone hält Kalman; spawn cap 2; yaw nil druckt. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.208 — 2026-09-08 (Pass 10)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 10 — gelandet

1. **Hung-Detect Cancel nach 1 s.** Vision-Detect hängt, `liveBusy` ewig, pending nur overwrite. `liveEmitHungCancel` + `liveDetectGen` invalidiert den stecken Task, `markFrameConsumed`, neuer Detect auf dem aktuellen Frame.
2. **Print-Cache leftoverLastHash × Yaw-Bin.** Gleicher Hash frontal übersprang VNFacePrint im Profil. `leftoverLastHashBinKey` / `leftoverPrintCacheHits` / `Put`. Skip nur gleicher Bin. uniqueID-Wechsel und `stopLive` wischen.
3. **JPEG-Probe × Yaw-Bin.** `leftoverJpegProbeStore` nur Spatial — Frontal-Probe auf Profil. `StoreBin` schreibt `#bin`, `LookupBin` Fallback Spatial nur Bin 0.
4. **Blink-Liveness auf Assign.** `leftoverAssignPrintCell` rief `leftoverAssignBlinkOk(..., alreadyNamed: true)` — Hungarian taufte Unnamed ohne Blinzeln. Zelle 0 wenn `blinkOk` fehlt (`alreadyNamed: false` intern). Call-Site `leftoverBlinkSeen(faceId:)`.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal.

Loop: drei Folge-Prüfungen — Hung 1 s startet neu; Profil nach Frontal druckt; Unnamed ohne Blink kein UUID.
