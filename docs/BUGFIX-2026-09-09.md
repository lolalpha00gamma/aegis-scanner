# Bugfix-Pass Aegis 2.1.248 — 2026-09-09 (Pass 50)

Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

2.1.247 leftoverPick twinPair nil + ROI/skipDetect every 2. Drei Live-Löcher blieben:

- `leftoverGhostAspectLock` blendete W und H **unabhängig**. Kalman WHV predW≠aspect → Box dehnt, Hash springt beim Nähern. Tests lockten linear 90/110.
- `leftoverLiveHash` rief `leftoverHoldWriteHash`. `leftoverTrailWriteHash` (Tests grün, Trail-Disk) tot.
- ROI/skipDetect every 2 @ 24 fps = 12 Hz Detect. Continuity 8 Hz braucht /2, Built-in 24 Hz braucht /4 — sonst MainActor-Hitch.

`leftoverPrintBudgetYawDelta` global **nicht** verdrahtet: Kommentar „Je UUID, nicht global“ — Twin dreht, Ada still würde sonst mitdrucken.

## Pass 50 — gelandet

1. **leftoverGhostAspectLock hypot-Aspect.** last Aspect, Size = hypot-Lerp. Blend 0 freeze. leftoverPredictHeld blend 0,25 unverändert.
2. **leftoverTrailWriteHash** in leftoverLiveHash. Alias = Hold-Hash, Call-Site sitzt.
3. **roiEvery aus dt.** Continuity ≥100 ms → 2. Built-in → 4. liveRoiPeriodicFull + leftoverDetectSkipTick gleiche Ticks. Defaults 8 bleiben.

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor, Overlay-Metal. leftoverHoldsTrack yawAbs: nil. leftoverPrintBudgetYawDelta Of je UUID. leftoverScore Twin-Penalty (frame-global, ändert Argmax nicht). leftoverPick twinPair nil (2.1.247).

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. VNImageRequestHandler(cvPixelBuffer:). Glasses On/Off. leftoverHoldsTrack yawAbs live (bewusst nil). Kalman R aus VN confidence. Privacy-Blur. Per-Camera Gallery.

Loop: Ghost kein Stretch; Trail-Hash live; Detect 8 Hz /2, 24 Hz /4. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.247 — 2026-09-09 (Pass 49)

Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

2.1.246 leftoverPick twinPair Live, Fallback `aegisHit?.pairCosine`. Ohne 32-d Vec (erstes Sample, skipPrints, Coast leer) → Gallery Ada-vs-Schwester 0,90. Same-shot Hard-Veto 0,88. Ada+Bob Overlay „?“ bis Prints da sind.

`liveRoiPeriodicFull` Default 8 = 1 s Walk-in. `leftoverDetectSkipTick every: 4` lässt Detect 3/4 tot — Full-ROI ohne Detect findet niemanden.

## Pass 49 — gelandet

1. **leftoverPick twinPair nil** ohne Live-Paar. Gallery-Centroid nicht mehr Fallback.
2. **liveRoiPeriodicFull every: 2.** Walk-in 250 ms. Default 8 bleibt.
3. **leftoverDetectSkipTick every: 2.** Detect + Full-ROI gleiche Ticks. Default 8 bleibt.

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor, Overlay-Metal. leftoverHoldsTrack leftoverPrintOk yawAbs: nil. leftoverScore Twin-Penalty Rank. leftoverPrintYawMerge printCommitted (2.1.245). leftoverPick Live-Twin (2.1.246).

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverHoldTrail Disk. leftoverHoldsTrack leftoverPrintOk yawAbs live. leftoverPrintBudgetYawDelta global (Of sitzt). VNImageRequestHandler(cvPixelBuffer:). Adaptive skipDetect.

Loop: Ada+Bob ohne Prints leftover darf. Walk-in 250 ms. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.246 — 2026-09-09 (Pass 48)

Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

2.1.245 leftoverPrintYawMerge printCommitted. leftoverPick **twinPair = Gallery-Centroid** (Ada vs Schwester 0,90). Zwei Live-Kisten Ada+Bob: leftoverTwinHardVetoNow 0,88, Gallery-Paar ≥ 0,88 → leftoverPick nil für **jede** Box. Overlay „?“, obwohl Live-Cosine 0,40.

## Pass 48 — gelandet

1. **leftoverPick twinPair Live.** Max paarweise leftoverCoastPrintCosine der adopted Vecs wenn faces ≥ 2. Gallery-Paar nur Fallback.

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor, Overlay-Metal. leftoverHoldsTrack leftoverPrintOk yawAbs: nil. leftoverScore Twin-Penalty Rank. leftoverPrintYawMerge printCommitted (2.1.245).

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverHoldTrail Disk. leftoverHoldsTrack leftoverPrintOk yawAbs live. leftoverPrintBudgetYawDelta global (Of sitzt). VNImageRequestHandler(cvPixelBuffer:). Adaptive skipDetect.

Loop: Ada+Bob Live 0,40 leftover darf. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.245 — 2026-09-09 (Pass 47)

Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

2.1.244 leftoverPickSameShot Rank. leftoverPrintYawMerge `printedIds: []` — Merge nie seedete, skip-print Yaw nach Stamp-Miss tot. printCommitted blieb auf der Source-UUID, leftoverHoldRemintDrop zog leftoverPrintYaw/Coast auf die Live-UUID: leftoverCoastPrintMerge `commitIds` skippte den neuen Print. leftoverLastHash nach leftoverCoastPrintWipe stale — leftoverOccupiedOthers belegte Ghost-Hashes, Exact-Hold tot. leftoverPrintYaw Filter nur live∪leftover, Coast-Keys weg. leftoverPeakHoldKeeps tot (Peak Guest `remaining>0`).

## Pass 47 — gelandet

1. **leftoverPrintYawMerge printedIds: printCommitted.** Stamp-Miss seedet. Nur Commit, kein Live-Copy aller Yaws.
2. **printCommitted leftoverHoldRemintDrop.** Commit-Set auf Live-UUID vor Merge. leftoverCoastPrintMerge commitIds trifft.
3. **printCommitted.insert(face.id).** Remint-UUID neben old.id.
4. **leftoverLastHash nach Coast-Wipe.** live ∪ leftoverCoastPrint ∪ (leftover ∧ ¬used). OccupiedOthers kein Ghost.
5. **leftoverPrintYaw hält leftoverCoastPrint.** Skip-print Yaw nach Dropout.
6. **leftoverOverlayPeakGuest leftoverPeakHoldKeeps.** miss = need − remaining. Peak-Guest Tests unverändert.

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor, Overlay-Metal, leftoverHoldTrail Disk. leftoverHoldsTrack leftoverPrintOk yawAbs: nil. leftoverScore Twin-Penalty. leftoverPrintBudgetYawDelta global (Of sitzt).

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverHoldTrail Disk. leftoverPrintBudgetYawDelta global. leftoverHoldsTrack leftoverPrintOk yawAbs live. leftoverScore Twin-Penalty in leftoverPickArgmax raw.

Loop: Stamp-Miss seedet Yaw; Remint-Commit trifft Coast-Merge; Coast-TTL LastHash weg; Peak-Hold Helper live. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.244 — 2026-09-09 (Pass 46)

Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

2.1.243 leftoverTwinSameShot = 0,88 nur HardVeto wenn facesInFrame ≥ 2. leftoverAmbiguousBlocks erlaubte Schärfe den Sieger zu drehen: 0,73 blur vs 0,72 scharf → Ada unter zwei Live-Kisten. pairCosine ist Gallery-Centroid (Ada vs Schwester), nicht zwei Live-Gesichter. Rank-Loch: zwei überlappende Boxen, pairCosine < 0,88, Schärfe wählt.

leftoverPickArgmax Score-Tie-Break bei Spread ≤ 0,08. leftoverOpenSetUnsure nur wenn arg==rawBest — Schärfe-Pick umging Unsure.

leftoverPrintYawMerge printedIds:[] bleibt no-op (Stamp committet). leftoverHoldsTrack yawAbs: nil bleibt Overlay-Policy.

## Pass 46 — gelandet

1. **leftoverPickSameShot.** facesInFrame ≥ 2.
2. **leftoverAmbiguousBlocks(..., facesInFrame).** Same-shot Spread < 0,08 immer Block, keine Schärfe-Wahl.
3. **leftoverPickArgmax sameShot.** Roh-Argmax, kein Score-Tie-Break.
4. **leftoverOpenSetUnsure** auch wenn sameShot (arg≠rawBest).

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor, Overlay-Metal, leftoverHoldTrail Disk. leftoverHoldsTrack leftoverPrintOk yawAbs: nil. leftoverPrintYawMerge printedIds: []. leftoverScore Twin-Penalty.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverHoldTrail Disk. leftoverPrintYawMerge printedIds: printCommitted. leftoverPrintBudgetYawDelta global (Of sitzt). leftoverHoldsTrack leftoverPrintOk yawAbs live.

Loop: zwei Live-Kisten Spread < 0,08 → nil, nicht Ada. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.243 — 2026-09-09 (Pass 44)

Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

2.1.242 leftoverCoastPrintWipe ohne skipPrints: Stamp freeze während Budget-Skip, TTL 2 s wischt Coast, leftoverOccupiedGhostDrop occupied — Exact-Hold tot.

leftoverGhostAspectLock bekam held.box.w/h = Predict. Freeze tot, Hash-Sprung beim Nähern.

leftoverSharpnessOf 420v tot: Continuity Laplacian 0,12 bleibt dunkel, leftoverBaptizeQuality Produkt unter Floor. Mac-Webcam 420v ohne Continuity-Flag ebenfalls. Tile-detectOnce ohne continuity.

leftoverHoldByHashSolo tot: leftoverHoldRemint rief nur leftoverHoldByHashRescue. Restart ohne leftoverLastHash, 1 Hold, Table-Key — x-Match Far, Hold tot. Solo nach leftoverHoldXMatch: Far-Pad stiehlt bevor Solo greift.

leftoverOverlayUniqueRows tot: Ghost+Live gleiche Row, SwiftUI ForEach dupliziert Ada.

## Pass 44 — gelandet

1. **leftoverGhostAspectLock Scale-Blend.** lastW/H = Kalman vor Predict, predW/H, blend 0,25. leftoverPredictHeld.
2. **leftoverSharpnessOf** FaceEngine detectOnce, videoRange: continuity || live. Tiles + Equalize. Mac 420v.
3. **leftoverHoldByHashSolo** in leftoverHoldRemint + leftoverHoldRemintBins **vor leftoverHoldXMatch**. Rescue bleibt danach.
4. **leftoverCoastPrintWipe skipPrints.** TTL-Arm, Exact-Hold frei.
5. **leftoverOverlayUniqueRows** ContentView overlayRows.
6. **liveRoiSkipOnWake in noteDidWake.** Helper live, nicht hardcoded true.

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor, Overlay-Metal, leftoverHoldTrail Disk. leftoverHoldsTrack leftoverPrintOk yawAbs: nil.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverHoldTrail Disk. leftoverTwinSameShot. leftoverPrintBudgetYawDelta (Of sitzt). leftoverHoldsTrack leftoverPrintOk yawAbs live.

Loop: Ghost wächst ohne Hash-Sprung; Live 420v Laplacian hebt; Restart Solo vor Far-x; skipPrints Coast bleibt; Overlay-Row einmal; Wake ROI Skip. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.242 — 2026-09-09 (Pass 43)

Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

2.1.241 leftoverTransfersId capture. leftoverPick leftoverOpenSetUnsure → nil, leftoverUnsureTicks nur `if holdUnsure`. leftoverOverlayGuestOf StoreName Ada zuerst. leftoverOverlayPeakName ohne remaining — SwiftUI nach Advance remain=0 hält Ada. leftoverCoastPrint Keys nach TTL: leftoverOccupiedGhostDrop all-or-nothing, Exact-Hold tot.

## Pass 43 — gelandet

1. **leftoverOverlayGuestOf openSetUnsure.** leftoverUnsureChip ?/??, Hist leer, vor StoreName.
2. **leftoverUnsureTicks** leftoverOpenSetUnsure neben holdUnsure.
3. **leftoverOverlayPeakName remaining.** leftoverOverlayGuest skip Peak bei ticks≥1.
4. **leftoverCoastPrintWipe.** live oder leftoverCoastPrintFresh. leftoverOccupiedGhostDrop ohne stale.

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor, Overlay-Metal, leftoverHoldTrail Disk. leftoverHoldsTrack leftoverPrintOk yawAbs: nil.

leftoverSessionCaptureBox Median + leftoverSessionCapturePrefersFrame sitzen in leftoverPick — nicht neu.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverHoldTrail Disk. leftoverTwinSameShot. leftoverGhostAspectLock Scale-Blend.

Loop: OpenSet „?“ nicht Ada; remain=0 kein Peak; Coast-TTL Exact-Hold frei. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.241 — 2026-09-09 (Pass 42)

Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

2.1.240 leftoverPick SessionCapture. leftoverTransfersId / leftoverAssignPrintCell / leftoverBaptizeQuality ohne capture — Nacht-Webcam Laplacian 0,14 Produkt < Floor 0,18, leftoverPrintOk hält 0,62. leftoverPredictHeld decayte WHV, leftoverGhostAspectLock(lastW: k.w) fror die Größe. leftoverHoldKalmanEncode nur vx,vy. LiveCapture AE vor startRunning — Helios-Bug 1.6.90.

## Pass 42 — gelandet

1. **leftoverBaptizeQuality capture.** FloorOf Nacht 0,06, leftoverPrintSharpOf.
2. **leftoverTransfersId + leftoverAssignPrintCell leftoverSessionCapture.**
3. **leftoverOverlayFirmName capture.** leftoverAssignPrintOk Nacht.
4. **leftoverFaceTrackPredictHeld pw/ph.** leftoverPredictHeld live. Ghost wächst.
5. **leftoverHoldKalmanEncode/Decode 12 WHV.** Restore boxKalmanWHV.
6. **applyCaptureLocks nach startRunning.** Wake/Mutex reassert.

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor, Overlay-Metal, leftoverHoldTrail Disk. leftoverHoldsTrack leftoverPrintOk yawAbs: nil.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverHoldTrail Disk. leftoverSessionCaptureBox Median. leftoverSessionCapturePrefersFrame. leftoverOpenSetEnergy Chip. leftoverCoastPrintFresh Key-Wipe.

Loop: Nacht 0,14 tauft; Ghost-Box wächst; Sleep AE steht. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.240 — 2026-09-09 (Pass 41)

Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

2.1.239 Kalman W/H, Firm-Name yaw, skip every 4. leftoverPick nahm weiter `sessionCapture: old.quality.capture` — Ghost-Tag 0,70 auf Live-Nacht 0,18. Floor bleibt Tag, Twin-Taufe.

`leftoverOccupiedLiveOnly` tot: leftoverCoastPrint Keys nach TTL halten Ghost-Hashes occupied — Exact-Hold tot.

`leftoverHoldSkipLookaway` nur Kommentar. Lookaway Yaw ≥ 0,28 bei HoldBin 0 (frontal bis 0,45) schrieb ¾-Print in die unbinned EMA.

`leftoverGhostIds` tot. `ghostIds = liveGhosts.map` ohne Dedup-Union.

## Pass 41 — gelandet

1. **leftoverSessionCapture** in leftoverPick. Live-min schlägt Ghost-Capture.
2. **leftoverOccupiedLiveOnly** wenn leftoverCoastPrintFresh alle leer/stale. Sonst storedDropped (kein leftoverOccupiedMerge auf #rank).
3. **leftoverHoldSkipLookaway** AND vor Hold-EMA.
4. **leftoverGhostIds(previous: dropped, ghosts: liveGhosts).**

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor, Overlay-Metal, leftoverHoldTrail Disk.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverHoldTrail Disk. leftoverSessionCaptureBox Median. leftoverSessionCapturePrefersFrame. leftoverOpenSetEnergy Chip.

Loop: leftoverPick Nacht 0,61 trotz Ghost 0,70; Coast-TTL Exact-Hold frei; Lookaway keine EMA; Dropout hält Ghost. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.239 — 2026-09-09 (Pass 40)

Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

2.1.238 leftoverHoldsTrack capture, Burst 3, Wake CS. Die Live-Löcher blieben:

- leftoverFaceTrackKalmanPredict nur x/y. W/H eingefroren — Nähern/Entfernen springt am Detect.
- leftoverOverlayFirmName leftoverAssignPrintOk ohne yawAbs/sharpness. Profil 0,81 zeigte „Ada“, leftoverBaptizeQuality sperrt ≥ 0,45.
- leftoverDetectSkipTick every 8. 7/8 Ticks Overlay freeze.
- Nacht leftoverPrintSharp 0,22: Laplacian 0,14 tot trotz leftoverSessionCapture Floor.

leftoverGhostAspectLock friert Ghost-W/H absichtlich. leftoverFaceTrackPredictHeld pw/ph default 0. leftoverHoldsTrack leftoverPrintOk yawAbs: nil bleibt (0,64 Profil hält Track). leftoverOverlayGuestOf Ada? Streak 2 unangetastet.

## Pass 40 — gelandet

1. **boxKalmanWHV.** skipDetect leftoverFaceTrackKalmanPredict pw/ph. Drop/Remint/Keep/Filter/Decay mit V.
2. **leftoverOverlayFirmName yawAbs + sharpness.** leftoverAssignPrintOk leftoverBaptizeQuality. Profil/Blur kein Firm-Name.
3. **leftoverDetectSkipTick every: 4.** Detect öfter. Function-Default 8.
4. **leftoverPrintSharpOf Nacht.** leftoverHoldsTrack 0,62 / Laplacian 0,14 hält.

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor, Overlay-Metal, leftoverHoldTrail Disk. leftoverGhostAspectLock. leftoverTransfersId capture. leftoverHoldsTrack leftoverSessionCapture (2.1.238).

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverHoldTrail Disk persist. Glasses On/Off. VNImageRequestHandler(cvPixelBuffer:). leftoverSessionCapture leftoverTransfersId. leftoverOverlayLerp. Adaptive skipDetect. leftoverAssignPrintOk capture.

Loop: skipDetect Box wächst; Profil kein Firm-Ada; Detect 4; Nacht 0,14 Overlay hält. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.238 — 2026-09-09 (Pass 39)

Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

`leftoverHoldsTrack` rief `leftoverPrintOk` **ohne capture**. Nacht-Frame 0,18 hielt den Tag-Floor 0,62 — Overlay lässt den Twin fallen, leftoverPick hätte 0,60. `enrollBurstReady` (3 Refs) tot: SM-Cache aus leftover-Prints war „ready“, Burst ersetzte die erste Referenz. `reconnectCenterStageOff` tot: Wake `force: true` unterschied Continuity nicht, Built-in CS-Pfad und Continuity-Reconnect liefen gleich.

## Pass 39 — gelandet

1. **leftoverHoldsTrack capture.** leftoverSessionCapture(old, live) → leftoverPrintOk. Nacht-Floor −0,02.
2. **enrollBurstReady vor Replace.** faceIds.count ≥ 3, nicht SM-Cache.
3. **reconnectCenterStageOff in recoverAfterWake.** Continuity CS an → aus. Built-in über needsReassert.

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor, Overlay-Metal, leftoverHoldTrail Disk, leftoverOpenSetEnergy Chip (Pick sitzt, Overlay-Guest bleibt leftoverUnsureChip).

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverHoldTrail Disk persist. Glasses On/Off. VNImageRequestHandler(cvPixelBuffer:). boxKalmanW/H Vel. leftoverOpenSetEnergy Unsure-Chip. leftoverTwinSameShot. Template-Aging.

Loop: Nacht 0,61 hält Overlay; Burst mit 1 Ref droppt Dup; Wake Continuity CS aus. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.237 — 2026-09-09 (Pass 38)

Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

2.1.236 skipDetect: `kalmanBox == vision`. leftoverPredictHeld und der async-kalmanSnap sind Double-ungleich — der else-Zweig adoptierte den alten Snap und wischte die Predict. Overlay oszillierte Predict/Snap. overlayHint „Profil“ ab 0,75, poseSlot ab 0,70 — 0,70–0,75 ohne Label.

## Pass 38 — gelandet

1. **leftoverDetectSkip(leftoverBoxIoU).** Coast 4‰ hält, Sprung adoptiert Vision-Track.
2. **overlayHint Profil ≥ 0,70.** poseSlot-gleich.

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor, Overlay-Metal, leftoverHoldTrail Disk.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverHoldTrail Disk persist. Glasses On/Off. VNImageRequestHandler(cvPixelBuffer:). boxKalmanW/H Vel. leftoverOpenSetEnergy Chip.

Loop: skipDetect coastet ohne Snap-Back; Profil-Hint ab 0,70. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.236 — 2026-09-09 (Pass 37)


Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

2.1.235 verdrahtete Kalman-Vel Sleep, PredictBoxes, enrollSMReady. Die Live-Löcher blieben:

- `geoVetoYawPrint == geoVetoSkipPrint == 80` — Yaw-Zweig tot. ¾ 72–79 % Geo-Veto.
- leftoverScore `/ 0,50` statt leftoverPrintProfileYaw 0,45. ¾ nur 56 % Strafe.
- skipDetect 7/8 Ticks: Zweig leer. Overlay friert, springt am Detect.
- FaceEngine `abs(quality.yaw)` nach Math-abs redundant.

## Pass 37 — gelandet

1. **geoVetoYawPrint 72.**
2. **Yaw-Strafe `/ leftoverPrintProfileYaw`.**
3. **skipDetect Kalman-Coast.** Seed → Predict, Vision → Vel.
4. **FaceEngine signed yaw.**

Loop: ¾ 79 % kein Veto; Score 0,45 == 0,50; skipDetect coastet. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.235 — 2026-09-09 (Pass 36)

Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

Nicht fehlende Schwellen. `leftoverFaceTrackKalmanVel`, `leftoverPredictBoxes`, `enrollSMReady` existieren, Tests grün — **keine Call-Site**. `boxKalmanVelocity` hat keine Sleep-Sperre: dt ≥ 2 s (App-Sleep) hält 45 % last-v, Overlay coaste in die falsche Richtung. `leftoverPredictHeld` rief `leftoverFaceTrackPredictHeld` pro UUID, `leftoverPredictBoxes` (Batch, 8-Hz-Coast) blieb tot. Burst-Dup ersetzte Referenzen **bevor** Front+¾L+¾R+Blink saß — drei Frontals, Coach tot. `leftoverHoldsTrack` rief `leftoverPrintOk` ohne explizites `yawAbs: nil`; nächster Pass hätte Profil-Floor 0,70 an den Overlay-Hold gehängt.

## Pass 36 — gelandet

1. **leftoverFaceTrackKalmanVel** vor boxKalmanV. Sleep dt ≥ 2 → Vel 0. Live bleibt EMA.
2. **leftoverPredictBoxes** in leftoverPredictHeld. Batch-Coast, Decay über leftoverHoldKalmanVelDecay.
3. **enrollSMReady** AND FromChip. Burst-Replace erst nach SM. leftoverLiveNameAnd dasselbe Gate.
4. **leftoverHoldsTrack leftoverPrintOk(yawAbs: nil).** Overlay 0,64 Profil hält Track. leftoverTransfersId behält yaw.

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor, Overlay-Metal, leftoverHoldTrail Disk.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverHoldTrail Disk persist. Glasses On/Off. VNImageRequestHandler(cvPixelBuffer:). leftoverYawPenalty 0,50 vs ProfileYaw 0,45. geoVetoYawSkip tot. leftoverBaptizeQuality live Overlay.

Loop: drei Folge-Prüfungen — Sleep kein Overlay-Sprint; Ghost coaste zwischen Detect; Burst ohne SM droppt; ¾ Overlay 0,64 hält. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.234 — 2026-09-09 (Pass 35)

Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

2.1.232 hat Spark/Pick/Lookaway signed. Die Math blieb unsigned:

- `leftoverPrintFloor`: `yaw >= 0,45` — −0,50 Profil L blieb Genuine 0,62. Twin-links tauft mit 0,62, Twin-rechts braucht 0,70.
- `leftoverBaptizeQuality`: `y >= 0,45` plus Product `1 - y / 0,45` — −0,50 invertiert Pose auf 1, Taufe offen.
- `leftoverScore` / `leftoverLiveWeight` / `centroidWeight`: `max(0, yaw / 0,50)` — negative Yaw = Strafe 0, Gewicht 1. ¾L scored höher als ¾R.
- Overlay-Chip: Store `abs(quality.yaw)` → leftoverHoldBinSigned → ¾L Chip ¾R.

## Pass 35 — gelandet

1. **leftoverPrintFloor / leftoverPrintOk abs.** Profil L Floor 0,70.
2. **leftoverBaptizeQuality / Product abs.** Profil L keine Taufe, Product L=R.
3. **leftoverScore / leftoverLiveWeight / centroidWeight abs.** Yaw-Strafe symmetrisch.
4. **leftoverHoldLabel signed.** Store reicht `quality.yaw`, Chip ¾L.
5. **geoVetoBlocks / geoVetoYawSkipped abs** (Defense; SkipPrint 80 macht den Yaw-Zweig tot).

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor, Overlay-Metal, leftoverHoldTrail Disk. leftoverHoldsTrack leftoverPrintOk ohne yaw (Overlay-Hold). leftoverTrailNowOf bleibt Wrapper auf leftoverHoldTrailOf (2.1.233).

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverHoldTrail Disk persist. Glasses On/Off. VNImageRequestHandler(cvPixelBuffer:). leftoverYawPenalty 0,50 vs ProfileYaw 0,45. geoVetoYawSkip tot. Kalman-Vel live. enrollSMReady. leftoverPredictBoxes.

Loop: drei Folge-Prüfungen — leftoverPrintFloor(−0,50)==0,70; leftoverScore L=R; Overlay-Chip ¾L. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.233 — 2026-09-09 (Pass 34)


Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

`leftoverAssignFillX` füllt nil-Zeilen per x-order — **keine Call-Site in leftoverAssignLive**. Nach Dropout ohne Embedding blieb Hungarian auf nil, leftoverPick taufte neu (Gast n+1 / Twin-Swap). Kommentar sagte „vor leftoverAssignDropAmbiguous“.

`leftoverTrailNowOf` und `leftoverHoldTrailOf` wählten ¾ vs Frontal doppelt. `leftoverShowsName` = leftoverBaptize, leftoverNameFromHold rief Baptize selbst. `leftoverEmptyKeepsStreak` tot neben emptyLatch.

`leftoverEmptyKeepsOverlay` tot: `leftoverPending = [:]` bei `!emptyChip` auch auf emptyLike/Miss-Coast — 2.1.78 Ghost-Kiste tot. `leftoverNeedsPrint` tot: Print-Budget skippt UUID ohne cosine, leftover-Pin stiehlt.

## Pass 34 — gelandet

1. **leftoverAssignFillX in leftoverAssignLive** vor DropAmbiguous. Twin-Spread bleibt.
2. **leftoverTrailNowOf → leftoverHoldTrailOf.**
3. **leftoverNameFromHold → leftoverShowsName.**
4. **leftoverEmptyKeepsStreak** an leftoverHoldSurvive (mit Latch).
5. **leftoverEmptyKeepsOverlay** vor leftoverPending-Wipe. emptyLike hält Ghost, Miss-Coast entscheidet später.
6. **leftoverNeedsPrint** filtert skipIdsAll. cosine nil → Print trotz Budget/Cache.

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor, Overlay-Metal, leftoverHoldTrail Disk.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverHoldTrail Disk persist. Glasses On/Off. VNImageRequestHandler(cvPixelBuffer:). Kalman-Vel live. enrollSMReady. leftoverPredictBoxes.

Loop: drei Folge-Prüfungen — Dropout ohne Print hält x-order; ¾-Trail nicht Frontal; Name nur nach Baptize; empty Frame hält Overlay-Ghost; UUID ohne cosine druckt. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.232 — 2026-09-09 (Pass 33)

Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

`leftoverHoldBinSigned` trennt ¾L (−1) und ¾R (+1). Die Live-Call-Sites schrieben `abs(yaw)`:

- `tickLeftoverSparkChips` → `leftoverSparkChipNow` → `leftoverHoldBinSigned` → ¾L liest ¾R-Trail.
- `leftoverPick` Dict `yawAbs[i] = abs(...)` → `leftoverHoldPrevOf` stiehlt den R-Hold.
- `leftoverLookawayYawOf(oldYaw: abs(...))` plus `leftoverLookawayBlocks` ohne `abs`: enrolled ¾L (−0,40) < 0,28 → nie WEG. Twin-rechts froze, Twin-links taufte.

## Pass 33 — gelandet

1. **Spark-Tick signed `quality.yaw`.** Overlay-Hold ¾L ≠ ¾R.
2. **leftoverPick `yawAbs` signed.** leftoverHoldPrevOf / leftoverTrailNowOf treffen das Vorzeichen.
3. **Lookaway `abs(yawAbs)`.** ¾L freeze wie ¾R. Ghost-Yaw signed, nicht abs.

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor, Overlay-Metal, leftoverHoldTrail Disk, leftoverHoldTrailOf (tot, leftoverTrailNowOf lebt).

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverHoldTrail Disk persist. Glasses On/Off. VNImageRequestHandler(cvPixelBuffer:). leftoverHoldTrailOf falten.

Loop: drei Folge-Prüfungen — ¾L Spark ≠ ¾R; enrolled Blick links WEG; leftoverPick stiehlt keinen R-Hold. Kein neues leftover*-Flag.

# Bugfix-Pass Aegis 2.1.231 — 2026-09-09 (Pass 32)

Kein Merge von `bugfix`. `[skip ci]`. Kein neues leftover*-Flag.

## Warum es schlecht funktionierte

Helios-Stamp ohne TTL: `applyMutexStampOnly` schrieb Palmen auch wenn Parse 12 s Stale war. Nach Continuity-Drop skippte skipPrint tote UV über Gesichtern.

`leftoverPrintCacheHits(..., cached: Set(leftoverPrintCache))` **und** `leftoverPrintCacheBins(Set(...))` je Track — O(n·m) Set-Allok im 8-Hz-Detect.

`burstPrev` / `burstAt` / `burstStreak` ohne Lock, Aufruf aus `Task.detached` — Burst-Reject jittert, Twin-Prints doppelt.

`stampPrints` rief `leftoverPrintSkipHits` zweimal (needPrint + Map).

## Pass 32 — gelandet

1. **Stamp-TTL 250 ms.** `cameraMutexStampFresh`. Palmen und Fill-PTS nur frisch. Stale → `[]` / 0.
2. **Print-Cache Set einmal** vor dem liveIds-Loop **und** compactMap.
3. **Burst-Reject unter `nmsLock`.**
4. **skipPrint-Hit-Array** einmal, Detect+Map lesen denselben.

Nicht angefasst: CameraBroker, HNSW, LiveCapture @MainActor (Detect bleibt detached), Overlay-Metal, leftoverHoldTrail Disk.

## Offen

CameraBroker XPC. HNSW Gallery. LiveCapture off MainActor. Overlay-Metal. leftoverHoldTrail Disk persist. Glasses On/Off. VNImageRequestHandler(cvPixelBuffer:).

Loop: drei Folge-Prüfungen — Helios-Crash 400 ms, Gesichter drucken; Print-Cache nicht lex; Burst ohne Doppel-Print. Kein neues leftover*-Flag.
